import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    resultImc: {
        flex: 1,
        marginTop: 15,
        borderRadius: 50,
        alignItems: 'center',
        width: '100%',
    },
    information: {
        fontSize: 18,
        color: '#ff0043',
        fontWeight: 'bold',
    },
});

export default styles;